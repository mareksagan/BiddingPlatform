pragma solidity ^0.5.0;

contract DoctorVote {
    string hello = "Hello World";

    function doctorVote(bytes memory data) public returns (string memory returnStatement)
    {
        return hello;
    }
}
